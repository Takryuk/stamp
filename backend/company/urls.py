from django.contrib import admin
from django.urls import path

# from rest_framework_simplejwt.views import (
#     TokenObtainPairView,
#     TokenRefreshView,
#     TokenVerifyView,
# )

# from .views import(
#     # purchase_video,
#     # username_change,
#     # profile_change, 
#     # image_change,
#     # PublicProfileView,
#     # UsernameChangeView,
#     # has_purchased,
# )

urlpatterns = [
    # path('purchase/', purchase_video, name='purchase-video'),
    # path('username/', username_change, name='username-change'),
    # path('profile/', profile_change, name='profile-change'),
    # path('image/', image_change, name='image-change'),
    # path('profile/<uuid:pk>',PublicProfileView.as_view(), name='public-profile')
    # path('has-purchased/<int:pk>', has_purchased, name='has-purchased'),

    # path('api/update', include('users.urls')),
    # path('api/token/', TokenObtainPairView.as_view(), name='token_obtain_pair'),
    # path('api/token/refresh/', TokenRefreshView.as_view(), name='token_refresh'),
    # path('api/token/verify/', TokenVerifyView.as_view(), name='token_verify'),
    # path('api-auth/', include('rest_framework.urls')),
]
